from setuptools import setup

setup(name='datasets',
      version='0.1',
      description='Convenience wrappers around several well-known datasets',
      url='http://github.com/overshiki/datasets',
      license='MIT',
      packages=['datasets'],
      zip_safe=False)
